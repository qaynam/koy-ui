{
  "name": "monorepo",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "ui:publish": "cd packages/ui && yarn build && yarn publish",
    "ui:storybook": "cd packages/ui && yarn storybook",
    "ui:storybook:build": "cd packages/ui && yarn build-storybook"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "turbo": "^2.3.3",
    "typescript": "5.5.4"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "yarn@1.22.22",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
