import Layout from './PageLayout.svelte';
import Title from './Title.svelte';
import Content from './Content.svelte';

const PageLayout = {
	Root: Layout,
	Title: Title,
	Content
};

export default PageLayout;
